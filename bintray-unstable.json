{
    "package": {
        "name": "unstable",
        "repo": "mesos-module-libstorage",
        "subject": "emccode"
    },

    "version": {
        "name": "${SEMVER}",
        "desc": "${DSCRIP}",
        "released": "${RELDTE}",
        "gpgSign": false
    },

    "files": [{
        "includePattern": "isolator/(.+-.+\.so)",
        "uploadPattern": "unstable/${SEMVER}/${MESOSVER}/$1"
    }],

    "publish": true
}
